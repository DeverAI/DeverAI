# done 2026-09-05 v8.28 — 备份范围控制 + 引用交付 + Agent+ 预设

- [x] A 备份范围控制：checkpoint_keep_per_file（默认 2 = 上两版）+ _max_per_file() 配置化 + Copilot 拦截等安全语义不变
- [x] B 换机环境提醒：DRIFT_ENV_RULES 随 drift.is_drifting() 注入（本地/服务器续算端同源）
- [x] C 网页变更栏：agent.js 采集文件类工具改动 → run_done changes → chat.js renderChangeLog
- [x] C 引用卡片：HTML=预览（iframe sandbox）/源码（默认折叠懒加载可复制）/文本=源码+复制/其余=仅文件
- [x] C 多图引用：扑克牌叠放 + mousemove 距离驱动排斥位移 + 悬停浮起 + lightbox 放大（/fs/image）
- [x] C2 云端交付纪律：sync_server_url 配置时注入「用户不能预览文件，必须引用形式交付」
- [x] D Agent+ 预设：AGENT_PRESETS（unattended/daily）+ apply_agent_preset + CLI --preset + 不看守 diff 预览自动放行
- [x] 测试：test_checkpoint_keep + unattended/keys 套件扩展 6 断言；py_compile 全量 0 失败；node --check 过
- [x] 收口：_audit_tmp gui+server exit 0 全绿；index.html ?v=8.28.0（17 处）；lite 孪生一致；Err.log=0
- [x] 阶段4：dev_log/20250905_v828.md + updates 归档 + 轮后备份 backups/20260905_v828_post/
- [x] E 上传文件功能：本轮交付设计方案（三选一），未实现（用户指示"想想"）
