# Future.md — 未来需求记录

- 多会话标签（当前为单会话持久化）
- 会话/工作区多端实时协同（v6 已以算力漂移快照推送/冷备续聊实现基础版，后续做实时双向）
- 冷备 Agent 的工具执行能力（~~当前仅续聊，不执行工具~~ → v8.27 已落地为「服务器普通端续算」：工作区素材化
  到 APPDATA + 密钥信封内存解密 + 不看守模式，sync_server 保持纯同步职责不内嵌 Agent）
- 图标/图片等视觉资产的尺寸改写（对话式增量修改）；入库与多维说明书已在 v6 落地
- 报错镜像生成期拦截加深（当前为系统提示注入，后续可做检索式精准拦截）
- 本地模型通道（Ollama 已天然兼容，后续做模型下拉即用）

## v8.5 网页版对齐工程（用户：网页版功能必须和普通版完全对齐）
- 批次2：模型注册表+三级匹配（char/bm25/api）、审批四模式、资产升级、建议去重、工具裁剪（对齐 desktop/models.py、matcher.py、vault.py）
- 批次3：专家团编排+四层树、toolsmith、notepad、err_mirror
- 批次4：browser/web_search、远程指挥、同步队列、后台任务、补全、命令面板、diff
- 遗留项（批次1 审查记录）：多标签页按 tab_id 隔离 guard/激活轮（当前按 username，多标签同用户会交叉）；轮内回退点三端点（rollback_point/rollback_points/rollback/{round_no}）前端未接线（批次1 commit 已按 guard 保留回退点）；审批挂起时停止按钮无效（Approval.request 与 abortCtrl 未关联，存量）；依赖树扫描对嵌套 node_modules 未排除（dep_tree 为桌面共用模块，改动需双端回归）。

## PPT 参考库&知识库（DeverAI 模块，v8.26 用户裁决「两个都要」的 DeverAI 半边）
- 资产银行扩展：用户允许的 PPT 入库（导出 PNG + 索引 + 多维说明书）；做 PPT 类任务时 AI 先圈阅库内 PNG 确定
  参考对象，再把相关切片/截图拷贝进上下文。
- 入库需用户批准；对接 file_protect 工作副本机制（原件不动，在 workcopy/ 副本上做 PNG 导出，规避「AI 禁碰
  用户资产」红线）。
- Mavis 侧同机制已先行落地（C:\Users\david\.minimax\ppt-library\ + ppt-library skill），本模块为其 DeverAI
  内化版（vault.py 扩展 + 双端 UI + agent 工具）。

## 上传文件功能（v8.30 用户裁决「按方案1做」——桌面端已落地；2/3 未做）
三选一方案：
1. 桌面拖拽入工作区（推荐）：文件从资源管理器拖进主窗口 → 拷入 uploads/<ts>-<名>/ → 走既有
   引用/备份/用户文件保护链路。成本最低，与「工作区即 Agent 运行地」语义一致。
2. 复用网页附件引用通道：聊天输入 @ 引用扩展「系统文件选择器」→ 桥端受控写入工作区
   （大小/类型门槛 + 敏感名拦截）。网页端可用，但新增受控写入口、攻击面变大。
3. 漂移通道反向带回：从服务器 APPDATA 素材位把文件带回工作区（适合云端/手机侧文件拉回本机），
   依赖 sync_server，非实时。
实现约束（任一方案都必须遵守）：入工作区即受用户文件保护/命名规则/一键备份覆盖；
uploads/ 不进 checkpoint 增量白名单（防 C 盘膨胀）；密钥类文件名拒绝入区。

落地记录（v8.30，方案1）：窗口级拖放 + 输入框 Ctrl+V 粘贴文件/剪贴板图片 →
uploads/<ts>-user/（uploads_ingest.py：敏感名拒绝、单文件 512MB 上限、同名加序号）→
自动挂引用条（指针式引用，AI 用 read_file 读取相对路径）；checkpoint 跳过 uploads/ 前缀。
远程端（用户追问「远程端怎么办」的裁决）：手机/另一台机器的文件入口**不并入方案1**——
走漂移反向带回（方案3 既有通道：推到服务器素材位 → 本机开机合并 / --from-drift）；
网页端受控上传=方案2，攻击面需单独审，另行立项。
