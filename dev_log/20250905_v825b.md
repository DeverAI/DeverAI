# dev_log 2026-09-05 v8.25b — 用户文件保护（补记）

> 本轮为**补记**：2026-09-05 下午（13:19–13:34）实际完成 file_protect 用户文件保护四端接线，但未写 dev_log、
> 未更新 todo/done、Design.md 未登记。v8.26 轮（同日晚）按工作流补记本文件，并把该轮遗留的 webui 死导入
> 缺陷一并修复（见 dev_log/20250905_v826.md）。

## 背景（用户需求）
1. 非Git环境下加强文件自动备份（WorkTree=checkpoint/session_snap/dep_tree/audit，不依赖git）。
2. PPT/Excel/Word/PDF 等用户改过的文件：AI 不允许执行相关命令或用工具覆盖，必须提示并要授权。
3. 用户一键备份完整工作区。
4. 多个文件名不清楚/重复时（copilot+worktree 发现奇怪点）自动要求识别、备份/转移。

## 改动清单（以源码为准）
- `pyqt/desktop/file_protect.py`（新建）：USER_SUFFIXES 用户资产后缀表、AI 写/命令触碰拦截、
  用户外部修改感知（data/file_protect.json 指纹 actor=ai→human）、backup_workspace_full 一键全量备份、
  detect_ambiguous 重名/命名不清扫描 + quarantine_files 隔离。
- `pyqt/desktop/checkpoint.py`：save_checkpoint_bytes 二进制快照（PPT/Excel 等原样存字节）。
- `pyqt/desktop/config.py` / `settings_dialog.py`：ENABLE_USER_FILE_PROTECT / ENABLE_FULL_BACKUP /
  ENABLE_AMBIGUOUS_GUARD 三开关。
- `pyqt/desktop/tools.py`：write/edit/delete 阻断接线 + backup_workspace / scan_ambiguous_files /
  quarantine_files 三工具。
- `pyqt/desktop/agent.py`：USER_FILE_PROTECT_RULES 提示词注入（桌面/网页共用语义）。
- `webui/app/bridge.py`：fs/write、run_command、fs/* 用户资产阻断。
- `webui/app/snap_bridge.py`：/protect/* 端点（备份/扫描/状态/隔离）。
- `webui/app/lite_server.py`：同源阻断（webui 内嵌 Lite 服务）。
- `webui/static/js/tools.js`：USER_ASSET_SUFFIXES 同源 + 前端拦截；`chat.js`、`lite.html` 孪生副本同步。

## v8.26 补检发现（重要）
- 该轮 webui 侧接线使用 `from desktop import file_protect`，而服务进程 sys.path 只保证 `pyqt.desktop`
  形式（bridge.py:35 _REPO_ROOT 前置）→ **webui 端用户资产拦截自落地起即为死代码**（写放行、命令不拦）。
  2026-09-05 晚 v8.26 轮冒烟实证（403 断言失败 + Err.log ModuleNotFoundError）并修复（详见 v8.26 dev_log）。
- 该轮未跑冒烟断言、未写 dev_log——FreqErr「收口验证必须在最后一次编辑之后执行」与 DevLog 纪律双双未满足，
  v8.26 轮已补齐（新端点/新工具均有真实调用断言）。

## 备份
- 轮前备份随 v8.26 轮一并落盘：`backups/20260905_v826_pre/`（file_protect.py 等修改前状态）。
