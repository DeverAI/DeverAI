# dev_log 2026-09-05 v8.26 — 编辑器统一 WorkTree 保护 + 工作副本 + 命名规则

## 背景（用户裁决）
1. **禁碰语义=拷贝**（用户原话「AI禁止碰不是不能碰，而是拷贝，原内容不修改，我们直接拷贝到其他地方干」）：
   用户资产从硬拦截升级为「拦截 + 引导拷贝工作副本」，原文件永不修改。
2. **编辑器**：用户拍板「DeverAI两端（Python+Web）两端统一」，Lite/DSH 插件明确不加；受 WorkTree 机制约束保护。
3. **PPT 参考库&知识库**：Mavis 侧固定库+技能先行落地；DeverAI 模块入 Future.md 下轮做（「两个都要」）。
4. **命名规则**：新建文件优先沿用工作区已有惯例；无惯例按「时间-作者-内容」。
5. **无工作区会话不摔桌面**：Mavis 侧行为规则（已记 user memory + DeverAI Fact.md 留档）。

## 改动清单
### A. 工作副本（禁碰=拷贝出去改）
- `pyqt/desktop/file_protect.py`：WORKCOPY_DIR + `_is_workcopy`（workcopy/ 首段豁免用户资产判定）、
  `make_workcopy(ws, rel, actor)`（时间-作者-内容命名 `YYYYMMDD-<actor>-<stem><ext>`，冲突 -2/-3，
  拒绝越界/绝对路径/副本目录内再拷贝，audit_log 留痕）、阻断文案统一追加 copy_user_asset 引导、
  sync_user_modified/detect_ambiguous 跳过 workcopy。
- `pyqt/desktop/tools.py`：`tool_copy_user_asset`（_readonly_block + ENABLE_WORK_COPY 门控 +
  _request_approval 审批门 + to_thread + file_changed 事件）；TOOL_FUNCS/build_tool_defs/TOOL_MATCH 三处注册。
- `pyqt/desktop/config.py` + `settings_dialog.py`：ENABLE_WORK_COPY 三层贯通。
- `webui/app/bridge.py`：`POST /api/bridge/fs/workcopy`（复用文件桥 _resolve/_is_protected 保护，
  file_protect 导入失败 501）。
- `webui/static/js/tools.js`：copy_user_asset def（`cfg.ENABLE_WORK_COPY !== false` 门控，与 v8.25 同约定）+
  dispatch + `execCopyUserAsset`（requestApproval → 端点 → Tree.refresh）。
- `pyqt/desktop/agent.py` + `webui/static/js/agent.js`：规则注入第5条（禁碰语义=拷贝出去改）+ 第6条（命名规则）。

### B. 编辑器两端统一 WorkTree 保护
- `webui/static/js/editor.js`：saveActiveTab 保存前（dirty 时）`POST /checkpoint/save {content:'', source:'human'}`
  （后端直读原文件，对齐桌面 gui.py save_current 的人类编辑快照语义）；标签栏新增「历史」按钮复用
  chat.js openVersionDialog（/checkpoint/versions + 预览 + 恢复前自动快照）。
- `pyqt/desktop/gui.py`：EditorWidget 新增 `versions_requested` 信号 + 编辑器右键菜单「版本历史…」→
  主窗 `_open_version_restore`（既有 VersionRestoreDialog，恢复前自动快照可二次回滚）。
- `webui/app/storage.py` + `bridge.py fs_write`：save_text 增加 crlf 参数 + `sniff_crlf`（读前 64KB 判定），
  写前按原文件行尾风格写回——消除网页端写文件丢 CRLF 的跨端漂移（对齐桌面 sniff_crlf 纪律），新文件 LF。

### C. 存量缺陷修复（本轮冒烟实证后顺手修复，均在 WorkTree/资产保护链路内）
- **webui `desktop` 别名死导入 ×8**：`from desktop import ...` 在服务进程恒 ModuleNotFoundError
  （sys.path 只有 repo 根，正确形式是 `from pyqt.desktop import ...`，bridge.py:35 注释即写明）且被
  try/except 静默吞掉 → v8.25 用户资产拦截在 webui 端自落地起为死代码、/checkpoint/* 端点 501。
  修复：bridge.py file_protect×4 + session_snap×1、snap_bridge.py checkpoint/session_snap/dep_tree×1 +
  file_protect×1、snap_util.py×1（补 sys.path 守卫）。FreqErr 已立新条目。
- **遗留同类 7 处暂不激活**（meta_bridge models/matcher/auto_score×3、proxy models×1、bridge toolsmith×3）：
  激活前需先解决 desktop.config 不认 DEVERAI_DATA_DIR 的数据目录隔离问题，避免冒烟/多实例污染真实
  pyqt/data——已在 Fact.md 登记为待办裁决。

### D. 测试与文档
- `tests/test_smoke_servers.py`：S8.5 工作副本冒烟（webui 专属：403+引导文案 / 副本创建命名 / 原件不动 /
  副本可写豁免 / 越界拒绝 400|403；Lite 独立版 server 级无守卫属预期，守卫在 lite.html 客户端）。
- `tests/test_desktop_offscreen.py`：`test_file_protect_workcopy` 12 项（创建/命名/内容一致原件不动/
  豁免判定/`workcopy/../` 穿越不豁免且仍阻断/仍拦截/文案引导/不存在拒绝/越界拒绝/副本内不再拷贝/
  config.json 敏感名拒绝/data 敏感目录拒绝；STATE_PATH 重定向防污染真实 data）。
- `Design.md`：§4.1 file_protect 行、§6.15（决策131-134）、§7.1/§7.2/§7.3 开关、§8 安全红线用户资产语义。
- `Techniques.md`：v8.26 章节 + v8.25b 补记章节 + 目录；`Fact.md`：v8.25/v8.26 裁决 + 版本事实 + 遗留登记；
  `Future.md`：PPT 参考库模块；`FreqErr.md`：desktop 别名导入恒失败条目。
- `webui/static/index.html`：?v= 全部 bump 8.26.0（17 处）。
- **Lite / DSH 插件零改动**（用户裁决）；lite.html 孪生副本 SHA256 保持一致。

### D2. 阶段3 全局故障检测（verifier 子AGENT）发现与修复
- [P0] `workcopy/../` 前缀借豁免绕过用户资产硬保护（`_is_workcopy` 只看首段）→ **已修**：拒绝 `..`
  穿越与盘符冒号，桌面/网页两端同源；补穿越断言。
- [P1] make_workcopy 无敏感防线，config.json/data/backups 可被拷入豁免区成外带通道 → **已修**：
  `_WORKCOPY_DENY_TOP`/`_WORKCOPY_DENY_NAME` 拒绝（data/backups/sessions/.git/.worktrees/缓存 +
  config.json/Err.log/api.txt/api_keys.py/.env）；补 2 项断言。
- [P1] webui ENABLE_WORK_COPY 三层断链 → **已修**：`ServerConfig.enable_work_copy` 后端强制校验
  端点（FreqErr「后端不强制前端开关」）+ core.js CFG_DEFAULT 补默认值；panels.js 开关 UI 与 v8.25
  族一致未展示（桌面设置为控制点，记录为债务）。
- [P2] webui 审批 dangerous:false 与桌面不等价 → **已修**：改 true。
- [P2] toolsmith 三处同类死导入 → **已修**（`pyqt.desktop`），遗留同类收敛为 meta_bridge/proxy 共 4 处。
- [P2] editor.js 保存前快照 dirty 条件 + 失败静默 → **已修**：无条件快照（外部改盘兜底）+ 失败 toast。
- [P2] Design 决策133 文档签名 eol→crlf → **已修**。

### E. Mavis 侧交付（仓库外）
- `C:\Users\david\.minimax\ppt-library\`：README（结构+入库/取用规则）+ index.json。
- `C:\Users\david\.minimax\skills\ppt-library\SKILL.md`：做 PPT 先查库圈 PNG → 相关切片进上下文；
  入库=用户允许后拷原件（只拷不改）+ PowerPoint COM 只读副本导出 PNG + 更新索引。
- User memory：AI 协作纪律三则（禁碰=拷贝 / 时间-作者-内容命名 / 无工作区会话整理）；
  Agent memory：PPT 库位置与用法。

## 验证（收口在最后一次编辑之后）
| 项 | 结果 |
|---|---|
| py_compile 全量 | 0 失败 |
| node --check tools/editor/agent.js | 全过 |
| lite.html 孪生 SHA256 | 一致（本轮未触碰） |
| tests/test_desktop_offscreen.py（Python 3.12.10 + PyQt6.11） | 全过 exit 0（含 workcopy 12 项） |
| python _audit_tmp.py（gui + server 双套件） | 全绿 exit 0（含 S8.5 webui 冒烟） |
| index.html ?v=8.26.0 | 17 处，8.25.0 残留 0 |
| Err.log | 0 字节（修复验证后清空） |
| 版本 | v8.26，环境备注：本机 PATH 上的 miniforge Python 3.14 与 PyQt6 不兼容，测试须用 `C:\Users\david\AppData\Local\Programs\Python\Python312\python.exe` |

## 备份
- 轮前：`backups/20260905_v826_pre/`（14 文件，_backup_tmp.py 拷贝）；轮后：`backups/20260905_v826_post/`。
