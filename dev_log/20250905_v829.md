# dev_log 2026-09-05 v8.29 — v8.27/v8.28 检修轮（读码→验证→修复→记录）

## 背景
用户要求对 v8.27/v8.28 已落地的七项需求做字面意义「检查」：不是冒烟，而是看代码、想到问题、验证、修复、记录；中途写好备份与文档更新。

## 审计结论（逐项）
1. 备份范围控制 — 桌面 checkpoint.py `_max_per_file()`（config `checkpoint_keep_per_file` 默认 2，夹 1-50）+ `_gc` 每文件裁剪 + `_MAX_TOTAL=600`；网页版经 snap_bridge `_desktop_modules()` 直接复用 desktop.checkpoint（同一 _gc，无第二实现，无双端漂移）。session_snap 只读停清理、孤儿回收、删除前审查、Copilot 拦截语义均未受影响。核对通过，无需修。
2. 换机环境提醒 — agent.py 在 `drift.is_drifting()` 时注入 DRIFT_ENV_RULES；退出链路 `begin_drift()`（gui.py:3732）先于 `_drift_push`（3739）→ drift_state.json(active=true) 随 data 包上服务器，`--from-drift` 将其合并进服务器 DATA_DIR → 续算端注入成立；漂移回本地 end_drift 后停止注入。链路闭合。
3. 变更栏+引用卡片（网页）— chat.js renderChangeLog/ref 卡片/扑克牌组/距离驱动排斥/lightbox 结构完整；agent.js run_start 重置采集、run_done 带 changes(≤50)；云端交付纪律按 sync_server_url 注入（panels.js 确认浏览器端 cfg 有该字段）。**发现 P1：/fs/image 返回 JSON 却被直接赋给 img.src——全部图片引用渲染失败**；且端点魔数只认 PNG/JPEG，IMG_EXT 的 svg/gif/webp/bmp 也会 415。→ 本轮修复（见下）。
4. Agent+ 预设 — AGENT_PRESETS/apply_agent_preset（dataclasses.replace 不污染原对象）、CLI --preset、settings SWITCHES 含 ENABLE_UNATTENDED；不看守审批最高优先（先于外交型路由）+ diff 预览自动放行。**发现 P2：cli_main --unattended 先置位、--preset 后 replace——daily 预设会把显式 --unattended 重置回 False（与注释宣称相反）**。→ 本轮修复。
5. 退出上传模式 — gui `_ensure_drift_upload_mode` 首问+记住（写 config `drift_upload_mode` 永久生效不再问），push_to_server upload="auto" → cfg.drift_upload_mode or minimal；退出等待 90s 对齐上传窗口。核对通过。
6. 断点暂停-上传-续算 + APPDATA — sync_server DRIFT_WS_DIR（%LOCALAPPDATA%\DeverAI\drift，SYNC_WS_DIR 覆盖）/素材化四件套/zip-slip 防护/256MB 实写帽/manifest 差集清理；/push 上限 192MB 与客户端 144MB 预算闭环；`--from-drift` 缺省 workspace=<dir>/workspace 且缺目录拒绝启动 + data 位合并（含 drift_state.json）。核对通过。
7. 密钥信封 — 内嵌盐 PBKDF2+Fernet(zlib)，无 cryptography fail-closed 返回空（拒绝降级混淆）；无口令不上传；服务器仅存密文；续算解密到内存不落盘。核对通过。本机未装 cryptography → fail-closed 不上传（装包后生效，requirements.txt 已含）。
其余核对：index.html 上轮 ?v=8.28.0×17（缓存纪律已遵守）；lite 孪生 SHA256 一致；上传文件功能=方案未实现且 **Future.md 缺归档**（v8.28 只留在对话里）→ 本轮补记。

## 修复清单
### A. [P1] 引用卡片图片直链修复（webui）
- bridge.py /fs/image 增 `raw` 模式：直接回图片字节（Response + content-type=mime）；raw 模式扩展 GIF/WebP/BMP 魔数；JSON 契约（ui_review base64）保持不变。
- chat.js `_imgUrl` 追加 `&raw=1`；IMG_EXT 移除 svg（魔数不含 svg），svg 进 TEXT_CODE_EXT 走「源码+复制」行。
- index.html `?v=` 全量 bump 8.28.0 → 8.29.0（17 处）。
- 新增 4 条冒烟断言（raw png 回字节 / raw gif 扩展魔数 / 非图片 415 / JSON 契约不变）。

### B. [P2] CLI 预设与 --unattended 顺序（桌面）
- cli_main.py：先套 --preset、后置 --unattended——显式标志不再被 daily 预设的 replace 吃掉；注释与行为对齐。

### C. [遗留补齐] 桌面端变更栏（v8.28 排「下一轮」，本轮落地）
- agent.py：`_changes` 采集（write_file/edit_file/delete_file/copy_user_asset；路径 meta.path→meta.copy→args.path 归一为 / 分隔；相邻重复 (path,action) 去重；run 每轮重置）；run 收尾一次性 emit `file_changes`（≤50 条）。专家/子Agent 的 file_changes 经 xemit 透传主 UI（xemit 只滤 run_start/run_done/run_final）。
- gui.py：ChatPanel `render_change_log`（QTextBrowser 卡片，点击锚 `act:openfile:<rel>` 经 `_on_anchor` 发 `open_file_requested` 信号 → MainWindow `_open_file` 编辑器打开）+ `_handle_event` 增 file_changes 分支。
- 桌面无「预览/源码」切换语义：本地可直接打开文件（与 Fact.md v8.28 遗留备注一致）。

### D. [文档] 上传文件功能三选一方案归档 Future.md（含实现约束）；FreqErr 新增「JSON 端点被当资源 URL 直用」条目。

## 测试
- 桌面：`test_changes_bar`（采集入列/工具事件正常，无 Qt）；test_gui 内变更栏渲染+去重+锚存在+点击信号断言（信号临时改接捕获，防真实打开副作用）。
- 冒烟：fs/image raw 4 断言（仅 webui 门控，lite 无 bridge 不误红）。
- 测试自身插曲：首版冒烟用了 `ws` 变量——该名在 worktree 段被 `client.post` 的 Response 覆写（TypeError: Response / str）→ 增 `ws_path` 引用原始 Path 后重跑全绿（变量复用教训，随 dev_log 记录）。

## 验证（收口在最后一次编辑之后）
| 项 | 结果 |
|---|---|
| py_compile 全量 | exit 0 |
| node --check chat.js/agent.js | exit 0 |
| tests/test_desktop_offscreen.py（含 changes_bar + 变更栏渲染断言） | exit 0 全过 |
| python _audit_tmp.py（gui+server，含 fs/image raw 4 断言） | exit 0 全绿 |
| index.html ?v=8.29.0 | 17 处、8.28.0 残留 0 |
| lite.html 孪生 SHA256 | 一致（本轮未触碰） |
| Err.log | 0 字节 |

## 备份
- 轮前 `backups/20260905_v829_pre/`（10 文件）；轮后 `backups/20260905_v829_post/`。

## 遗留
- 上传文件功能：方案已归档 Future.md，未实现（待用户在三选一中裁决）。
- 多图扑克牌 nth-child(6+) 全叠同一位置（挤压+排斥位移可用、点击放大可用），后续可做动态 spread。
- 桌面变更栏图片走「打开文件」；QTextBrowser 内嵌缩略图/HTML 预览排后续（如需要）。