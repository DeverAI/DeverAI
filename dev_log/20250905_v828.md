# dev_log 2026-09-05 v8.28 — 备份范围控制 + 引用交付 + Agent+ 预设

## 背景（用户六项）
①备份范围控制（C 盘爆红 → 每文件保留上两版；WorkTree Copilot 拦截危险操作语义不变）②上传/换机提醒注入上下文 ③变更栏 + 引用卡片（预览/源码/仅文件，源码默认折叠可展开可复制；多图扑克牌式压缩+悬停距离驱动排斥+点击放大）④配置自动上云漂移时告知 AI 用引用形式交付 ⑤Agent+ 模式预设（无人值守即一种预设，把能干的活先干完）⑥上传文件功能——本轮只出方案。

## 改动清单
### A. 备份范围控制（config/checkpoint）
- `checkpoint_keep_per_file: int = 2`（可配 1-50）；checkpoint.py `_max_per_file()` 读配置（`_KEEP_OVERRIDE` 供测试），`_gc` 按配置裁剪每文件版本（原固定 20）。session_snap 上限、删除前 AI 审查、Copilot 拦截等安全语义不变。

### B. 换机环境提醒（agent.py）
- `DRIFT_ENV_RULES`：漂移状态（`drift.is_drifting()`，本地与服务器续算端同源——服务器 data 位含 drift_state.json）下注入「依赖/路径/服务可能需重配，先验证环境，做不了保留进度」。

### C. 变更栏 + 引用卡片（webui：agent.js/chat.js/style.css/index.html）
- agent.js：主循环采集文件类工具改动（write/edit/delete/mkdir/rename/copy_user_asset，run_start 重置、去重）→ `run_done` 携带 `changes`；`buildSystemPrompt` 增【云端交付纪律】（配置 sync_server_url 时注入：用户可能无法预览工作区文件，必须用引用形式交付）。
- chat.js：`renderChangeLog` 变更栏卡片——HTML=「预览」（iframe sandbox allow-scripts 独立渲染）+「源码」（默认折叠、懒加载、可复制）、「源码+复制」（文本/代码）、「仅文件」（无按钮默认态）；图片走 `/fs/image`；多图=扑克牌叠放（mousemove 距离驱动排斥位移+悬停浮起+点击 lightbox 放大），单图=缩略图+放大。
- style.css：`.chlog/.ref-*` 全套样式（扑克牌 nth-child 错位叠放）。
- index.html `?v=` 全量 bump 8.28.0。

### D. Agent+ 预设（config/cli_main）
- `AGENT_PRESETS`（unattended=无人值守「禁提问、非危险自动放行、把能干的活先干完」/ daily=日常）+ `apply_agent_preset`（dataclasses.replace 不污染原对象）+ CLI `--preset`（显式 --unattended 仍可覆盖）。
- 不看守模式下 `_request_diff_approval` 自动放行（diff 预览需人工确认，无人值守不阻塞）。

### E. 测试
- desktop 套件新增 `test_checkpoint_keep`（保留 2 版裁剪）；`test_unattended_and_keys` 扩展 6 断言（敏感名不入包/cutoff 基准/manifest/增量筛选）。

## 验证（收口在最后一次编辑之后）
| 项 | 结果 |
|---|---|
| py_compile 全量 + node --check agent/chat.js | 0 失败 |
| tests/test_desktop_offscreen.py（含 checkpoint_keep + v8.27 16 项） | exit 0 全过 |
| python _audit_tmp.py gui+server | exit 0 全绿 |
| index.html ?v=8.28.0 | 17 处、旧版本残留 0 |
| lite.html 孪生 SHA256 | 一致（本轮未触碰） |
| Err.log | 0 字节 |

## 备份
- 轮后 `backups/20260905_v828_post/`（v8.28 改动 11 文件；本轮为连续同日开发，v8.27_post 即隐式轮前基线）。

## 遗留（登记 Fact.md）
- 变更栏/引用卡片本轮落地**网页端**；桌面端变更栏 UI（QTextBrowser 内交互受限，拟走「打开文件+对话框」路径）排下一轮。
- 上传文件功能方案见本轮对话结论（三选一：桌面拖拽入工作区 / 复用网页附件引用通道加大小与类型门槛 / 漂移通道反向带回），未实现。
