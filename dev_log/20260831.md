# dev_log 2026-08-31 — v8.19 全量检修轮

> 用户指令：「全量检修优化，不限范围，不限操作类型。记得及时阶段性备份+维护文档」。
> 本轮为检修轮（无新功能）：基线验证 → 修复 → 静态全检 → 深度巡检 → 回归 → 备份/文档。
> 全程零 git 操作；备份走 backups/20260831_v819/。

## 一、基线验证（阶段0）

- audit_report.md（历史深度审计）逐项复核：sync_server_url 快照排除、cli_main /mode 守卫、
  冒烟 terminate 守卫等均在早前轮已修复；`sync_server.py` 幽灵依赖确认为**文档漂移**——
  Fact.md 2026-08-24 环境事实记载该文件存在于另一台机器，本工作区副本刻意不含；
  Design.md §3 入口表与 §4.5 文件表已加注记（见下）。
- 跑双套件建立基线，**当场抓出三个真实故障**（见二）。

## 二、修复清单（按严重度）

1. **[Critical] tests/test_desktop_offscreen.py 悬空 `try:`（IndentationError）**
   - 现象：main() 中 `try:` 后紧跟未缩进的 `try:`，py_compile 直接报
     `expected an indented block after 'try' statement on line 258`——
     整个桌面回归套件当前完全不可运行（v8.18 dev_log 声称"全过"与实际不符，
     收口验证显然跑在最终编辑之前）。
   - 修复：删除悬空 `try:` 行，恢复 `try: test_term_envs() except ...` 结构。

2. **[Critical] webui/app/bridge.py 缺失 `_save_term_envs` 定义**
   - 现象：`_save_term_envs` 在 4 处被调用（term_create/term_delete/resolve_and_touch/
     run_command last_cmd 回写）但从未定义 → NameError → term/create 500；
     冒烟测试中 webui 进程随后连接被强关（WinError 10054）。
   - 根因闭环：Err.log 残留 00:52:04 的 NameError 堆栈与本诊断完全一致。
   - 修复：补 `_save_term_envs(data)` = `save_json(TERM_ENVS_PATH, data)`（原子写）。

3. **[High] 冒烟测试对 lite 断言 v8.18 终端环境池（过度断言）**
   - 现象：lite 的 term/create 404（lite 刻意不实现 v8.18，见 v8.18 裁决四端清单），
     但测试对两版本统一断言 → 永久红。
   - 修复：smoke_version 增加 `expect_term` 参数，term 族断言仅 webui 启用。

4. **[Medium] webui/static/index.html 缓存破坏版本号未随 v8.17/v8.18 更新**
   - 现象：fs.js/tools.js/agent.js 仍 `?v=8.13.0`、panels.js 仍 8.16.0，但四个文件在
     v8.17/v8.18 均有改动 → 浏览器旧缓存 JS 静默缺新功能。
   - 修复：四文件统一 bump 至 `?v=8.19.0`。

5. **[文档漂移] Design.md / Fact.md / audit_report.md**
   - Design.md 六处 PyQt5 → PyQt6（迁移早完成，文档未跟）。
   - Design.md §4.5 列出的 tests/test_smoke.py 等四个旧测试文件与 tools/svg_picker.py
     均不存在 → 改为当前真实文件表 + 历史注记；§3 sync_server 行加"未随本仓库分发"注记。
   - Fact.md 双入口条目 PyQt5 → PyQt6（历史裁决条目保留原貌）。
   - audit_report.md 头部补处置状态（逐项标注已修/文档漂移结论/本轮新增修复）。

6. **[Low] pyqt/cli_main.py 过时 `# noqa: F401`**——log_error 实际被使用，移除错误注释。

## 三、深度巡检（全过，零缺陷）

- 危险正则五处同源逐条比对：pyqt/desktop/tools.py、webui/app/security.py、
  lite/app/security.py、webui/static/js/tools.js、lite/static/lite.html（合并式正则）——
  模式清单逐行一致，严格布尔处理一致。
- 模块开关三层贯通：config.py ENABLE_* 字段 → tools.build_tool_defs 裁剪 →
  settings_dialog.SWITCHES，无缺口；桌面 term 三工具在 TOOL_HANDLERS/build_tool_defs/
  TOOL_KEYWORDS 三处注册齐全。
- AGENTS.md 故障库 8 项回归抽检：goal_reached 严格判定（modes.py 否定词排除）、
  remote_cmd danger_ok 门（is_dangerous_cmd + _strict_bool）、lite.html 无 document 级
  click 监听、SSE limit=1024*1024、CRLF sniff_crlf + 原子写、多会话切换（测试覆盖）。
- 双 lite.html 孪生逐字节一致（SHA256 相等）。

## 四、验证

- 桌面离屏回归：51 项全过（修复语法错误后恢复可运行）。
- 双服务端冒烟：全过（term 族断言 webui 门控后 lite/webui 双绿），Err.log 复核为
  本轮已修复的 NameError 残留 → 清空保留空文件。
- 静态：全仓 .py 编译 0 失败、全部 js node --check 通过。

## 五、备份与文档

- backups/20260831_v819/：改动前测试×2 + 改动后 bridge.py、cli_main.py、index.html、
  Design.md、Fact.md、audit_report.md（共 8 文件）。
- FreqErr.md 新增 3 条：「收口验证时序」「同文件并行 Edit 竞态」「四端功能版本门控」。
- AGENTS.md 项目故障库新增 1 条：测试文件语法损坏逃过收口。
- todo.md/done.md 生命周期流转。

## 六、教训（记入 FreqErr）

1. v8.18 收口声明"测试全过"，但最终代码留有语法错误——收口验证必须跑在**最后一次编辑之后**。
2. 同一文件并行发多个 Edit 工具调用会互相覆盖（本轮 index.html fs.js 版本号被回滚后重修）。
3. 冒烟测试按版本门控功能断言：lite/webui 能力差异是裁决结果，不是测试对象。
