# dev_log 2026-09-05 v8.32 — 全端代码检查轮（读码→验证→修复→记录）

## 背景与方法
用户指令：「字面意义上的'检查'，不是冒烟而是看代码、想到问题、验证、修复、记录；中间及时备份&文档更新；
同步各端功能（除 deepseek harness 插件）；Lite 为内存基本保持不变；分歧谁新谁强用谁」。
范围=四端全量（pyqt 桌面 / webui 前后端 / lite / CLI）。基线为 v8.31 落地后的脏工作区
（git status 26 M + 22 ??），轮前先打基线备份提交。

## 查实并修复（8 项）
### F1 [P2] note_ai_write 全仓库零调用 → AI 生成资产被永久误锁（五端接线）
- file_protect.note_ai_write（AI 写入登记 actor=ai 指纹）定义后从未接线：AI 生成的 .csv 等
  资产文件无记录，check_ai_write_block 内 sync_user_modified 会把它补记 human/user_modified，
  AI 下次编辑被「用户文件保护」永久拦截；「AI 刚生成可继续改」分支永远走不到。
- 修复：桌面 tool_write_file / tool_edit_file 成功后登记（编辑后必须刷新，否则 AI 自己的编辑
  被误判为用户修改）；webui bridge fs_write、webui lite_server fs_write、lite lite_server fs_write
  同步接线。仅 is_user_asset(rel) 时登记，防状态文件被普通文本写爆。
- 回归：test_note_ai_write_flow 5 断言（默认拦截→登记放行→用户修改识别→重新锁死→非资产不受影响）。

### F2 [P2] desktop.config 不认 DEVERAI_DATA_DIR（v8.26 遗留前置解除）
- webui/lite 均支持 env 重定向数据目录，唯 desktop.config 硬编码 pyqt/data——冒烟/多实例会污染
  真实数据，也是 v8.26「遗留 4 处死导入暂不激活」登记的前置条件。
- 修复：与 webui/lite 同款 env 重定向（未设置时行为不变）。
- 回归：冒烟 POST /api/bridge/models 后断言 models.json 落在 DEVERAI_DATA_DIR（端到端锁死）。

### F3 [P2] meta_bridge×3 + proxy×1 死导入激活（v8.26 遗留本体）
- `from desktop import ...` 在 webui 服务进程恒 ModuleNotFoundError 且被吞：/api/bridge/models*
  恒 501、proxy 注册表 url/key 覆盖从未生效。
- 修复：四处改 `from pyqt.desktop import`。激活面核实：models/matcher/auto_score 导入面纯
  stdlib + .config/.storage/.llm（无 Qt/numpy），server 进程可安全加载。
- 回归：冒烟 GET/POST/DELETE /api/bridge/models 200×3。

### F4 [P2] Lite 后端用户资产保护缺失（webui 副本有、lite 副本无）
- lite.html 前端有 isUserAsset/commandTouchesUserAsset 拦截，但 lite lite_server 后端零
  file_protect 引用——裸调 API 可整体绕过（FreqErr「保护只做桥端」模式）；webui 的 lite_server
  副本 v8.25 已补，lite/ 副本漏同源。
- 修复：fs/write 补 check_ai_write_block、run_command 补 command_touches_user_asset（403），
  按「谁新谁强」从 webui 副本移植；Lite 无 UI/路由新增，符合轻量约束。
- 回归：冒烟 Lite fs/write 资产 403 + 命令触碰资产 403。

### F5 [P3] drift 上传包密钥后缀旁路
- sync.build_drift_upload excl_name 只有 5 个文件名，.pem/.key/.pfx/.p12 可绕道进漂移包；
  uploads_ingest 注释声称「与 sync.build_drift_upload excl_name 同源」实际并不同源。
- 修复：excl 校验补密钥后缀（任意层级）。
- 回归：test_sync_excl_key_suffix 4 断言（根/.pem、子目录/.key、普通文件照常、zip 内容双确认）。

### F6 [P3] 两个 lite_server 副本 fs/write 不保行尾
- bridge.py v8.26 已按 sniff_crlf 纪律写回，webui/lite 两个 lite_server 副本漏同源——
  Lite 编辑 CRLF 文件会被整体改写行尾。
- 修复：lite/app/storage.py 补 sniff_crlf + save_text(crlf)（与 webui 版同名同语义）；
  两个 lite_server fs/write 接 crlf=sniff_crlf(p)。lite.html 孪生副本未触碰（SHA256 前后一致）。
- 回归：冒烟 Lite CRLF 写入保持断言。

### F7 [P3] ENABLE_MODES 死开关接线（FreqErr「新功能字段只有配置无消费」）
- ENABLE_MODES「三大模式」在 config/设置页之外零消费——关掉开关三大模式照常生效。
- 修复（9 处消费面）：modes.is_mode_active、agent 系统提示词注入（traffic/token）、context
  激进压缩、experts 高级升级、planner 规划/汇总、tools.select_tools 裁剪、sync_queue 流量挂起、
  gui._check_sleep_goal 关机倒计时；设置页三个模式勾选随总开关禁用。
- 保留：聊天区模型弹窗的模式芯片可继续切换（写配置值），总开关关闭时不生效——UI 全套隐藏排
  后续（避免本轮动大块 UI，见遗留①）。

### F8 [P3] powershell -enc 危险正则四端统一为 \b 强形式（新锁首跑抓到真实漂移）
- 新增 test_dangerous_patterns_four_end_sync：从五份源码（desktop/security×2/tools.js/lite.html）
  抽取 DANGEROUS_PATTERNS 做集合比对（JS \/ 与 [\s\S] 归一后比对；lite.html 顶层交替切分）。
- 首跑即抓到：lite.html 的 `powershell\s+-enc` 带 \b 词边界，其余三端没有——按「谁强用谁」
  四端统一为 \b 强形式（tools.py + security.py×2 + tools.js），并按缓存纪律 bump index.html
  ?v=8.29.0→8.32.0（17 处，tools.js 改动的强制动作）；node --check 过。

## 顺带核实（不动）
- 四端危险正则 28 条现逐条一致（静态锁固化）；USER_SUFFIXES 四端一致（均 28 项）。
- webui/lite ServerConfig 差异仅 enable_work_copy（v8.26 裁决 Lite 不接，刻意）。
- agent.py 取消语义健康（3 处 CancelledError 均重抛+历史落账）；checkpoint v8.30/v8.31 修复在位；
  snap_bridge 保护谓词在位；sync_server 唯一 tmp + web-cold 格式保持在位；storage 原子写/fsync 三端一致。
- webui 无 ENABLE_USER_FILE_PROTECT 开关（恒开）vs 桌面有开关——按「谁强用谁」保留 webui 恒开（更安全方向），记录差异（见遗留②）。
- uploads_ingest.alloc_dest 并发同名竞态（exists 检查后 copy 覆盖）——桌面单线程拖拽入口实际低危，记录不修（见遗留③）。
- webui 前端 js 无 .strip() 残留；switchEl outerHTML 教训注释在位。
- CLI（cli_main.py）P1/P2 修复链完整（diff 分支/审批锁/取消落账/错误落 Err.log）。

## 修复过程中发现并修正的测试问题（2 项，均为测试侧）
- 新增断言误用桌面套件 2 参 check() 为 3 参 → TypeError 两处（自测自纠）。
- 「增量基准：cutoff 后新文件入包」间歇红（pwsh-18 绿、19/复跑红）：隔离复现 6/6 恒绿 →
  判定为测试墙钟脆弱（time.time 与 NTFS mtime 可被时钟步进反转，FreqErr #193 同族），产品侧
  drift 增量跨机比较必须用墙钟、语义不动；测试内显式 os.utime 锚定 mtime 修确定性。
  按 FreqErr #211「区分应用缺陷与测试预期写错」归类为后者。

## 验证（收口在最后一次编辑之后）
| 项 | 结果 |
|---|---|
| py_compile 全量 | exit 0 |
| tests/test_desktop_offscreen.py（+11 新断言：指纹5+drift排除4+四端正则锁6 中2计数并入） | exit 0 全过 |
| python _audit_tmp.py（gui+server 双套件） | exit 0 全绿 |
| 冒烟新增断言（Lite 后端保护×2 + CRLF + models 三连 + 隔离） | 7/7 [OK] |
| lite 孪生 SHA256 | 前后一致（lite.html 全程零改动） |
| index.html ?v= | 8.32.0×17，旧版残留 0（tools.js 改动触发 bump） |
| node --check tools.js | exit 0 |
| Err.log | 0 字节 |

## 备份与记录
- 轮前基线提交 d988bee「backup: v8.31 dirty-state baseline before v8.32 check round」
  （59 文件快照；v8.24「检修前 git init 建本地基线快照」先例）。说明：工作区红线禁止用命令行
  复制项目文件，故本轮未做 backups/<ts>_pre/ 手工副本——19 个编辑文件中含 4000 行级大文件，
  read+write 搬运成本高且 git 基线已逐字节覆盖全部编辑文件；后续恢复点=git 基线或任一历史版本。
- FreqErr：新增「AI 写入指纹登记缺失」；#119 遗留清零更新；#163 补静态锁说明。
- Fact.md：追加 v8.32 检查轮条目（含遗留待表态三项）。
- AGENTS.md：过时版本注记修正（8.23.0→8.32.0）+ 故障库补两条（指纹误锁 / 墙钟脆弱测试）。

## 遗留与建议（未决，待用户表态）
1. 聊天区模式芯片未随 ENABLE_MODES 隐藏（行为已门控，仅 UI 观感）——是否全套隐藏？
2. webui 无 ENABLE_USER_FILE_PROTECT 开关（恒开）vs 桌面有开关——补 ServerConfig 字段+设置同步，还是接受差异？
3. uploads_ingest.alloc_dest 并发同名竞态——桌面单入口低危；网页端上传（方案2）立项时必修。
4. test_smoke_servers 每轮真实起 3 个 uvicorn+mock LLM（~90s）——要不要加跳过开关提速日常轮？
5. 桌面 config.py 的 load_json 只吞 FileNotFoundError/JSONDecodeError，PermissionError 会裸抛——调用点多有 try 包裹，是否在 storage 层统一兜底？
