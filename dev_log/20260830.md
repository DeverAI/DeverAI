# dev_log 2026-08-30 — v8.17 安全中心轻档聚合

> 本轮裁决：轻档（纯聚合现有安全能力为可视化 UI，不改底层删除语义）+ 三版本对齐 + 不做回收站。
> 全程零 git 操作。

## 一、v8.17 安全中心

### 后端最小改动
- `webui/app/security.py` + `lite/app/security.py`（孪生同步）：新增 `read_audit(tail=200)` 与 `clear_audit()` 两个纯函数
- `webui/app/server.py`：新增 `GET /api/security/audit` + `POST /api/security/audit/clear`
- `lite/app/lite_server.py`：新增相同孪生端点 + 补齐 register/login 的 `security.audit()` 调用（此前 lite 侧注册/登录不写审计日志）
- 无新模块开关——安全中心始终可见

### 三版本前端
- **桌面版** `settings_dialog.py`：新增 Security Tab（命令/文件/网络安全状态卡片 + allow_ai_delete/power_authorized/llm_allow_loopback 开关聚合 + 审计只读列表 + 导出/清空 + Err.log 查看/清空）
- **网页版** `panels.js`：SETTINGS_NAV 新增 security 条目 + `renderSecurityPage()` 函数（状态卡片 + 审计流水 + 导出/清空 + 错误日志）
- **Lite** `lite.html`（孪生同步）：chat-head 新增 🛡 安全按钮 + 全屏弹窗审计日志查看/清空

### 验证
- 静态：89 py 编译 + 15 js node --check + 双 lite.html 逐字节一致——全过
- 桌面离屏 `tests/test_desktop_offscreen.py`：38 项（+3：SettingsDialog Security Tab 构造/切换/audit.list_audits 返回列表）——全过
- 服务冒烟 `tests/test_smoke_servers.py`：42 项（+4：审计读取 200/含注册记录/清空/清空后为空）——全过
- Python 环境说明：本机 Python 3.14（miniforge3 默认）无 PyQt6/uvicorn/fastapi，测试须使用 Python 3.12（`C:\Users\david\AppData\Local\Programs\Python\Python312\python.exe`）

## 二、文档更新

- Design.md：§6.12（决策 119-121）
- Techniques.md：目录项 + v8.17 章节（需求背景/A/B/C 三节）
- Fact.md：v8.17 裁决条目
- done.md + todo.md：生命周期流转

## 三、备份

- `backups/20260830_v817/`：security.py×2、server.py、lite_server.py、settings_dialog.py、panels.js、lite.html×2、test 文件×2、文档×4

---

# dev_log 2026-08-30 — v8.18 终端环境池 + 悬浮小助手 Agent 循环 + 系统专家

> 本轮裁决：① 命令行新增持久环境（cwd+env 轻量会话）+ 更新间隔心跳 + 任务后杀进程，全部带默认值 warning；② 悬浮小助手（小龙）升级为 Agent 循环入口（一次 LLM 调用自判 chat/agent_loop/system_expert），主循环软暂停；③ 系统专家模式（只读查阅 Design/Techniques/Fact 回答系统问题）；④ 四端同源（webui 后端 + webui 前端 + 桌面后端 + 桌面前端）。全程零 git 操作。

## 一、终端环境池（v8.18）

### 后端
- `webui/app/bridge.py`：新增 `/api/bridge/term/create|list|delete` 三个端点 + `_term_resolve_and_touch` 辅助；`run_command` 扩展 `env/update_interval/kill_after` 三参数；`update_interval>0` 时分片等待 + 心跳事件（K210 刷写静默场景）；`kill_after=false` 时超时不杀树（left_running 标记）；默认值 warning 作为首个 SSE 事件 + 输出首行下发。持久化 `data/term_envs.json`（原子写）。
- `pyqt/desktop/terms.py`（新模块）：桌面端独立实现终端环境池（与 bridge.py 结构对齐但不共享代码），`create_env/list_envs/get_env/delete_env/resolve_env_cwd_env`，线程锁 + 原子写。

### 前端 / 工具注册
- `webui/static/js/fs.js`：`bridgeRunCommand` 透传 env/updateInterval/killAfter，捕获 warnings/leftRunning/update 事件。
- `webui/static/js/tools.js`：run_command schema 扩展三参数；新增 `term_create/term_list/term_delete` 工具定义 + exec 分支 + switch 注册；warnings 注入输出首行。
- `pyqt/desktop/tools.py`：`tool_run_command` 扩展参数解析 + warnings；`_exec_command` 重写（env 合并 / 心跳 / kill_after / warnings）；新增 `tool_term_create/list/delete`；注册 TOOL_HANDLERS + build_tool_defs + TOOL_KEYWORDS。

## 二、悬浮小助手 Agent 循环（v8.18）

- `webui/static/js/agent.js`：新增软暂停原语（`agentPauseMain/agentResumeMain/agentWaitIfPaused`），工具循环顶部 + 每个工具执行前两个停靠点；新增 `runPetAgent(userText, opts)` 独立 Agent 循环（独立 abort 控制器 / 独立 messages / 不污染主历史 / 无 SnapRound·saveHistory·sleep 副作用）。
- `webui/static/js/voice-pet.js`：`processInput` 无意图分支改为 `decideAndProcess`（一次 LLM 调用自判 `{mode: chat|agent_loop|system_expert}`）；`runPetLoop`（暂停主循环 → runPetAgent → 恢复）+ `runPetExpert`（系统专家 prompt）+ `renderPetEvent`（工具事件渲染到悬浮面板）。

## 三、测试

- `tests/test_desktop_offscreen.py`：新增 `test_term_envs`（创建/列表/解析/重复名/非法变量/删除/持久化）。
- `tests/test_smoke_servers.py`：新增 term_create/list/delete + run_command(env/update_interval/kill_after/warnings) 端到端。

## 四、文档更新

- Design.md：§6.x（决策 122-124）
- Techniques.md：v8.18 章节
- Fact.md：v8.18 裁决条目

## 五、备份

- `backups/20260830_v818/`：bridge.py、terms.py、fs.js、tools.js×2、agent.js、voice-pet.py、test×2、文档×4

