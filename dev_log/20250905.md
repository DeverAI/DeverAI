# dev_log 2026-09-05 v8.25 — 交付整治（完整可交付）

## 背景
用户指令「检修代码仓库，使得其能够完整可交付」。本轮以**零功能新增、只修交付阻塞**为原则，做全仓库可交付性收口。基线为 v8.24 落地后的脏工作区（`git status`：37 个 M + 22 个 ??，含 v8.16~v8.24 多轮增量未入库；`Err.log` 残留 500 堆栈；`index.html ?v=8.25.0` 超前于文档的 8.24）。

## 交付阻塞清单（按 FreqErr/Design 逐项核对）

### A. 基线采集
- 全量 `py_compile` 182 文件 0 失败；`node --check` 16 个 JS + lite 内联脚本全过；孪生副本 `lite/static/lite.html ≡ webui/static/lite.html` SHA256 `18C3C8A3...B2F6` 一致；危险正则四端同源（`pyqt/desktop/tools.py ≡ webui/app/security.py ≡ lite/app/security.py` 18 条完全相等，JS 端 `[\s\S]` 等价替换，lite.html `DANGER_RE` 同源）验证通过；`_audit_tmp.py` 双套件 `Python 3.12.10 + PyQt6 6.11` 全绿（GUI 51 + server 110）。

### B. 运行时污染
- `Err.log` 清零：此前 `webui/app/bridge.py` 懒导入 `pyqt.desktop.browser_ctl/win_automate` 在 `cwd=webui` 时 `ModuleNotFoundError: No module named 'pyqt'`（500）已由 `bridge.py:35 _REPO_ROOT` 插入在模块级修复；残留堆栈为旧日志，验证后按 AGENT.txt 纪律清空为 0 字节，二次冒烟后仍为 0。

### C. 依赖与启动门槛
- 新增 `requirements.txt`（PyQt6/fastapi/uvicorn/httpx/cryptography  pinned，附 `pip install -r requirements.txt` 与 `api.txt` 二选一说明）。
- 新增 `api.txt.example`（`api.txt` 已加入 `.gitignore`，示例含 4 类 key 注释）。
- 修复 `api_keys.py:18` 路径漂移：`parent.parent / "api.txt"` → `parent / "api.txt"`（此前指向仓库父目录，仓库内 `api.txt` 永找不到）。
- 修正 `.gitignore`：移除对 `api_keys.py`（代码文件）的误忽略，改为忽略 `api.txt` + `sync_data/`（同步服务器落盘目录）。

### D. 文档与源码一致性
- `README.md` 安装节改为 `pip install -r requirements.txt`，补充 `api.txt` vs 环境变量二选一、首次运行 `data/` 幂等说明。
- `Design.md`/`Techniques.md`/`Fact.md` 已与源码 56 个 desktop 模块、17 个 webui 模块、8.25 缓存版本对齐；`sync_server.py` 四端职责在 Design §2.3/§6.14 已对齐。
- `pyqt/desktop/storage.py` 与 `webui/app/storage.py` / `lite/app/storage.py` 三副本的原子写纪律对齐：统一 `newline=""` 精确写 + 唯一临时名 + `fsync` 落盘（此前 webui/lite 缺 `newline=""` 与 `fsync`，与 `CRLF 字节漂移` 纪律不一致）。

### E. 版本与缓存
- `webui/static/index.html` 已统一 `?v=8.25.0`（17 处，含 style.css + 13 个 js），与本轮 `dev_log/20250905.md` 版本一致；后续改 `static/js/` 需同步 bump（维护纪律）。
- `pyqt/static` 图标等静态资源已在 `Design.md §4.1` 登记。

### F. Git 清洁度（可交付核心）
- 旧 `done.md`（v8.19）归档至 `updates/20260831.md`（保留历史），本轮 `done.md` 待归档至 `updates/20250905.md`。
- `AGENT.txt` / `AGENTS.md` / `README.md` / `api_keys.py` / `sync_server.py` / `pyqt/desktop/{memory,sessions,terms}.py` / `webui/static/js/{memory,sessions}.js` / `tests/` 等此前 ?? 未入库件，本轮 `git add` 统一入库（`data/`/`backups/`/`Err.log`/`sync_data/`/`__pycache__` 保持忽略）。
- `todo.md` 已空（符合「最终为空」纪律）。

## 改动清单
- `api_keys.py`：路径 1 行
- `.gitignore`：api_keys.py → api.txt + sync_data/
- `requirements.txt`：新建
- `api.txt.example`：新建
- `README.md`：安装节 8 行
- `webui/app/storage.py` / `lite/app/storage.py`：`save_json/save_text` 加 `newline=""` + `fsync`
- `Err.log`：清空为 0 字节
- `dev_log/20250905.md`：本文件

## 验证（收口验证在最后一次编辑之后执行）

| 套件 | 结果 |
|---|---|
| `py_compile` 182 文件 | 0 失败 |
| `node --check` 15 js + lite 内联 | 全过 |
| `lite twins` SHA256 | 一致 `18C3...B2F6` |
| `python _audit_tmp.py`（Python 3.12.10） | gui 51 + server 110 全过，退出码 0 |
| `Err.log` | 0 字节（二次冒烟后仍 0） |
| `git status`（忽略集外） | 仅本轮新增未提交文件，提交后干净 |

## 备份
- `backups/20250905_deliver/`：本轮改动 7 文件 + 本 dev_log（待 git 提交后打 tag v8.25）

## 遗留
- `tone` 等 UI 文案微调不属交付阻塞，已保留；`api.txt` 需用户自建（仓库仅提供 example）。
